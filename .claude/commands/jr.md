---
description: job-radar (alias). See /job-radar.
argument-hint: "[scan|triage|show ID|eval ID|apply ID|render ID|status|contact|touch|followup|jd|export|db|import|init] [...]"
---

Alias of `/job-radar`. Route the user's request to the right `jr` subcommand.

See `/job-radar` for the full routing table.

User's request: $ARGUMENTS
